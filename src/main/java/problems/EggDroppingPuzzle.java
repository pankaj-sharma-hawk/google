package problems;

public class EggDroppingPuzzle {

    public static void main(String[] args) {
        /*Scanner sc = new Scanner(System.in);
        int test = sc.nextInt();
        while (test-- > 0) {
            int n = sc.nextInt();
            int k = sc.nextInt();
            System.out.println(Math.round(Math.log(k) / (n % 2 == 0 ? Math.log(2) : Math.log(3)) + 0.5));
        }*/
        System.out.println(Math.log(28) / Math.log(2));
    }
}
